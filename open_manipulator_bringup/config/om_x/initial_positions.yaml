# Configuration for Open Manipulator X initial positions

joint_trajectory_executor:
  ros__parameters:
    joint_names:
      - joint1
      - joint2
      - joint3
      - joint4

    step_names: [ready]  # Names of the steps

    ready: [0.0, -1.0, 1.0, 0.0]  # Ready position

    duration: 3.0
    epsilon: 0.01
    action_topic: '/arm_controller/follow_joint_trajectory'
    joint_states_topic: '/joint_states'
